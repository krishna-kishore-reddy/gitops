apiVersion: monitoring.coreos.com/v1
kind: Alertmanager
metadata:
  name: monitoring-kube-prometheus-alertmanager 
  namespace: monitoring
spec:
  replicas: 1
  version: v0.28.1
  # Reference a Secret with alertmanager.yaml
  configSecret: alertmanager-main
